// Create the database

// Insert into database

// Get fromn database

// Delete from database

// const { Favorites } = require('../models/favorites');


// Favorites.findAll().then(users => {
//     console.log('All users:', JSON.stringify(users, null, 4));
// });
